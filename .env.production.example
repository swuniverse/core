# Star Wars - HoloNet - Production Environment Variables
# Kopiere diese Datei zu .env.production und passe die Werte an

# ============================================
# PostgreSQL Datenbank
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ÄNDERE-MICH-SICHERES-PASSWORT  # openssl rand -base64 24
POSTGRES_DB=swholo_game

# ============================================
# Backend Configuration
# ============================================
NODE_ENV=production
PORT=3000

# Database Connection String
# WICHTIG: Passwort muss mit POSTGRES_PASSWORD übereinstimmen!
DATABASE_URL=postgresql://postgres:ÄNDERE-MICH-SICHERES-PASSWORT@postgres:5432/swholo_game?schema=public

# JWT Authentication
# Generiere mit: openssl rand -base64 32
JWT_SECRET=GENERIERE-MIT-openssl-rand-base64-32
JWT_EXPIRES_IN=7d

# Redis Connection
REDIS_HOST=redis
REDIS_PORT=6379

# CORS Configuration
# WICHTIG: Setze auf deine Domain oder IP!
CORS_ORIGIN=https://deine-domain.com  # oder http://deine-ip

# Asset Repository URL
VITE_ASSET_BASE_URL=https://swholonet.github.io/assets

# ============================================
# Game Settings (Optional)
# ============================================
TICK_INTERVAL=60000  # 60 Sekunden in Millisekunden

# ============================================
# Debugging (Production: false)
# ============================================
DEBUG_LOGGING=false  # true für detailliertes Logging

# ============================================
# Admin Invite Code (Optional)
# ============================================
# Wird beim ersten Start automatisch generiert
# Du findest ihn in den Backend-Logs
ADMIN_INVITE_CODE=

# ============================================
# Sicherheits-Hinweise
# ============================================
# 1. JWT_SECRET MUSS geändert werden (mindestens 32 Zeichen)
# 2. POSTGRES_PASSWORD MUSS geändert werden
# 3. Beide Passwörter in DATABASE_URL übereinstimmen
# 4. CORS_ORIGIN auf echte Domain/IP setzen
# 5. Diese Datei NICHT in Git committen (.gitignore prüfen)

# ============================================
# Secrets generieren
# ============================================
# JWT Secret:
#   openssl rand -base64 32
#
# Starkes Passwort:
#   openssl rand -base64 24
#
# UUID:
#   uuidgen

