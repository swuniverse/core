# Produktions-Umgebungsvariablen
# Kopiere diese Datei zu .env.production und passe die Werte an

# Database
POSTGRES_PASSWORD=dein-sicheres-db-passwort-hier

# JWT Secret (generiere mit: openssl rand -base64 32)
JWT_SECRET=dein-sicherer-jwt-secret-hier

# CORS Origin (deine Domain)
CORS_ORIGIN=https://deine-domain.com

# Optional: Admin Invite Code
ADMIN_INVITE_CODE=ADMIN001
