# Star Wars - HoloNet - Production Environment Variables
# WICHTIG: Kopiere diese Datei zu .env.production und passe die Werte an
# Oder nutze das Setup-Script: bash .env.production.setup.sh
#
# SICHERHEIT:
# - Alle Passwörter/Secrets sollten mit 'openssl rand -base64 24' oder 'openssl rand -base64 32' generiert werden
# - .env.production sollte niemals in Git gepusht werden (steht in .gitignore)
# - Speichere die Werte sicher ab für zukünftige Deploys

# ============================================
# PostgreSQL Datenbank
# ============================================
POSTGRES_USER=postgres
# Generiere mit: openssl rand -base64 24
# WICHTIG: Dieses Passwort wird automatisch URL-encoded für die DATABASE_URL
POSTGRES_PASSWORD=ÄNDERE-MICH-SICHERES-PASSWORT
# Docker Service-Namen (in docker-compose)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ============================================
# Backend Configuration
# ============================================
NODE_ENV=production
PORT=3000

# Database Connection String
# WICHTIG: Das Passwort MUSS automatisch URL-encoded sein!
# Falls du das Script .env.production.setup.sh nutzt, wird das automatisch gemacht.
# Ansonsten: Nutze https://www.urlencoder.org um spezielle Zeichen zu encoden
# Beispiel: / wird zu %2F, + wird zu %2B, = wird zu %3D
DATABASE_URL=postgresql://postgres:ÄNDERE-MICH-SICHERES-PASSWORT-URL-ENCODED@postgres:5432/swholo_game?schema=public

# JWT Authentication
# Generiere mit: openssl rand -base64 32
JWT_SECRET=GENERIERE-MIT-openssl-rand-base64-32
JWT_EXPIRES_IN=7d

# CORS Configuration
# Setze auf deine Production Domain
CORS_ORIGIN=https://swholo.net

# Redis Connection
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# Asset Repository URL
VITE_ASSET_BASE_URL=https://swholonet.github.io/assets

# ============================================
# Game Settings (Optional)
# ============================================
TICK_INTERVAL=60000

# ============================================
# Debugging (Production: false)
# ============================================
DEBUG_LOGGING=false
# Du findest ihn in den Backend-Logs
ADMIN_INVITE_CODE=

# ============================================
# Sicherheits-Hinweise
# ============================================
# 1. JWT_SECRET MUSS geändert werden (mindestens 32 Zeichen)
# 2. POSTGRES_PASSWORD MUSS geändert werden
# 3. Beide Passwörter in DATABASE_URL übereinstimmen
# 4. CORS_ORIGIN auf echte Domain/IP setzen
# 5. Diese Datei NICHT in Git committen (.gitignore prüfen)

# ============================================
# Secrets generieren
# ============================================
# JWT Secret:
#   openssl rand -base64 32
#
# Starkes Passwort:
#   openssl rand -base64 24
#
# UUID:
#   uuidgen

